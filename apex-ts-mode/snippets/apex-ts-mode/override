# -*- mode: snippet; require-final-newline: nil -*-
# name: override
# key: o
# --
${1:$$(yas-choose-value '("private" "public" "protected" "global" ""))} override ${2:return} ${3:Name} (${4:params}) {
    $0
}
