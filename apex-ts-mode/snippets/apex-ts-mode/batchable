# -*- mode: snippet -*-
# name: batchable
# key: bat
# --
${1:$$(yas-choose-value '("private" "public" "protected" "global" ""))} ${2:$$(yas-choose-value '("with sharing" "without sharing" ""))} class ${3:class} implements Database.Batchable<SObject> {
    $0
}
